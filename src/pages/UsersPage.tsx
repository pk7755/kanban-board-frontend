/**
 * UsersPage.tsx
 * Placeholder — full implementation in Phase 5.
 */

export function UsersPage() {
  return (
    <main style={{ padding: 'var(--space-6)', color: 'var(--text-primary)' }}>
      <h2>User Management — coming in Phase 5</h2>
    </main>
  )
}
